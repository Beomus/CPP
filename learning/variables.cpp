#include <iostream>

using namespace std;

int main() {
    // variable assignment start with type
    string name = "bruv";

    // does not have to assign immediately
    int age;
    age = 3;
    cout << "hello " << name << endl;
    cout << age << endl;
    return 0;
}